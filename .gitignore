vite.config.*.timestamp*
vitest.config.*.timestamp*
test-output

# compiled output
dist
tmp
out-tsc

# dependencies
node_modules

# IDEs and editors
/.idea
.settings/
*.sublime-workspace
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# misc
/coverage
npm-debug.log
yarn-error.log

# System Files
.DS_Store
Thumbs.db

# Nx
.nx/cache
.nx/workspace-data
.cursor/rules/nx-rules.mdc
.github/instructions/nx.instructions.md
.claude/worktrees
.claude/settings.local.json
.nx/polygraph
.nx/self-healing

# --- Project-specific ---
# Secrets — never commit the real environment file (SC-009)
.env

# Python
.venv/
venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Nx Python plugin reports
/reports

# Documents
docs/*
!docs/adr
!docs/product-backlog.md

# Skills
.claude/skills/thesis-write-style
.claude/skills/thesis-write-style-workspace
.claude/skills/research-papers

# Evaluation harness — real corpus contains real PII (RODO/GDPR): never commit (EPIC 8, D6).
# Ignore the directory contents but keep the dir on clone via the .gitkeep negation.
apps/gateway-eval/gateway_eval/corpus/data/real/*
!apps/gateway-eval/gateway_eval/corpus/data/real/.gitkeep
# Machine-readable per-document results may embed originals — treat as sensitive.
apps/gateway-eval/eval-results/
